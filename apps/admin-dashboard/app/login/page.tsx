import { AdminPage } from "@admin/components/admin-page";

export default function AdminLoginPage() {
  return (
    <AdminPage
      eyebrow="Admin access"
      title="Login"
      description="Authenticate with elevated credentials before accessing management workflows."
    />
  );
}
