import { AdminPage } from "@admin/components/admin-page";

export default function AdminCategoriesPage() {
  return (
    <AdminPage
      eyebrow="Catalog"
      title="Categories"
      description="Manage the category tree that drives storefront navigation and filters."
    />
  );
}
