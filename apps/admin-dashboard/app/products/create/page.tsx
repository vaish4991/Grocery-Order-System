import { AdminPage } from "@admin/components/admin-page";

export default function CreateProductPage() {
  return (
    <AdminPage
      eyebrow="Catalog"
      title="Create product"
      description="Use this form to add a new catalog item, pricing, stock, and media metadata."
    />
  );
}
