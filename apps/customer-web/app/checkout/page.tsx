import { ContentPage } from "@customer/components/content-page";

export default function CheckoutPage() {
  return (
    <ContentPage
      eyebrow="Shopping"
      title="Checkout"
      description="Address selection, coupon entry, order summary, and payment handoff happen here."
    />
  );
}
