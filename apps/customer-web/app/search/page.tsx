import { ContentPage } from "@customer/components/content-page";

export default function SearchPage() {
  return (
    <ContentPage
      eyebrow="Search"
      title="Find products quickly"
      description="Search results will later connect to filters, autocomplete, and full-text search across the catalog."
    />
  );
}
